import { ContainerBase, Layout } from "../components";

const Editor = () => {
  return (
    <Layout>
      <ContainerBase>Hola</ContainerBase>
    </Layout>
  );
};

Editor.auth = true;

export default Editor;
